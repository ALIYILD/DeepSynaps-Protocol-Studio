# Evidence DeepDive Final Report — 2026-05-09

Generated by coordinator task t_f341f371 (DeepDive 4/4 — Validate + Final Report).

---

## Summary

The Evidence page 4-part deep-dive is complete. All code changes were committed across two branches
(backend+DB on `agent/coordinator/t_013ee166`, frontend wiring on `agent/coordinator/t_a588fec1`).
Validation passes across all test suites. Push to remote is blocked pending GitHub auth (SSH key /
HTTPS token not configured in this environment) — manual push required before PRs can be opened.

---

## What Shipped (by phase)

### Phase 1 — Research + Architecture (t_6d3dda03)
- Produced `docs/evidence-deepdive-architecture.md` (310 lines, commit 964a8b33)
- Defined Class A + Class B scope; identified 6 backend gaps, 4 frontend gaps
- Branch: `agent/coordinator/t_6d3dda03`

### Phase 2 — Backend + DB (t_013ee166)
- Commit: 3801d1b8, branch: `agent/coordinator/t_013ee166`
- 6 files changed:
  - `apps/api/app/services/evidence_service.py` — new `get_indication_detail()` returning papers + grade + FDA note
  - `apps/api/app/routers/evidence.py` — new `GET /api/v1/evidence/indications/{slug}` endpoint
  - `apps/api/app/models/indications.py` — added `grade`, `fda_note`, `papers` fields
  - `apps/api/alembic/versions/20260509_evidence_indication_detail.py` — migration adding columns
  - `apps/api/app/services/indications_seed.py` — seeded `tdcs_asd` (Grade C, no FDA clearance note) + `tps_chronic_pain` (Grade D, CE-for-Alzheimer-only note)
  - `apps/api/tests/test_evidence_indication_detail.py` — 40 backend tests

### Phase 3 — Frontend + Evidence wiring (t_a588fec1)
- Commit: e7ec86aa, branch: `agent/coordinator/t_a588fec1`
- 4 files changed:
  - `apps/web/src/pages-research-evidence.js` — new `_renderEvidenceLinkedClaims()` with PMID/DOI chips; `_renderPaperRow` enhanced; fallback banner uses clinician-judgment wording
  - `apps/web/src/pages-research-evidence-frontend.test.js` — 12 new tests
  - `apps/web/src/evidence-live-wiring-regressions.test.js` — 8 regression tests (3 new, 2 stale assertions fixed)
  - `apps/web/package.json` — 3 test files registered in `test:unit`

### Phase 4 — Validation + Final Report (t_f341f371)
- This document
- Ran full validation suite (see below)
- No new code changes needed — all prior work validates cleanly

---

## Test Results

| Suite | Tests run | Pass | Fail |
|-------|-----------|------|------|
| Full web unit suite (`npm run test:unit`) | 1220 | 1220 | 0 |
| Page-specific JS tests (evidence + live-wiring) | 24 | 24 | 0 |
| Backend pytest (`uv run pytest apps/api/tests/test_evidence*`) | 40 | 40 | 0 |
| **Total** | **1284** | **1284** | **0** |

Build: `npm run build` — completed in 7.24s, 0 errors.

---

## PR Status

| Branch | Commit | Status |
|--------|--------|--------|
| `agent/coordinator/t_013ee166` | 3801d1b8 | Local only — push blocked (no GitHub auth) |
| `agent/coordinator/t_a588fec1` | e7ec86aa | Local only — push blocked (no GitHub auth) |
| `agent/coordinator/t_f341f371` | (this commit) | Local only — push blocked (no GitHub auth) |

GitHub auth constraint: SSH key permission denied; HTTPS requires token not set in environment.
Manual push command (run from each worktree with a configured GitHub token or SSH key):

```
# Backend PR
cd /Users/aliyildirim/hermes-agent/.worktrees/t_013ee166
git push origin agent/coordinator/t_013ee166
gh pr create --draft --repo ALIYILD/DeepSynaps-Protocol-Studio \
  --title "feat(evidence): DeepDive 2/4 — backend + DB (indication detail endpoint)" \
  --body "Adds GET /api/v1/evidence/indications/{slug}, grade/fda_note/papers columns, tdcs_asd + tps_chronic_pain seeds. 40 tests pass."
# After tests pass: gh pr ready --repo ALIYILD/DeepSynaps-Protocol-Studio <PR_NUMBER>

# Frontend PR
cd /Users/aliyildirim/DeepSynaps-Protocol-Studio/.hermes_worktrees/t_a588fec1
git push origin agent/coordinator/t_a588fec1
gh pr create --draft --repo ALIYILD/DeepSynaps-Protocol-Studio \
  --title "feat(evidence): DeepDive 3/4 — frontend evidence-linked claims + clinician-judgment states" \
  --body "Adds _renderEvidenceLinkedClaims(), PMID/DOI chips in _renderPaperRow, clinician-judgment fallback banners. 24 frontend tests pass. No fabricated identifiers."
# After tests pass: gh pr ready --repo ALIYILD/DeepSynaps-Protocol-Studio <PR_NUMBER>
```

---

## Demo-Readiness

**Demo-ready: YES (local / staging)**

The Evidence page is demo-ready in a local or staging environment where both backend and frontend
branches are deployed together:
- Indication detail endpoint returns real Grade + FDA note + papers list (no fabricated data)
- Frontend renders PMID/DOI chips when present; shows "No evidence — clinician judgment required" when absent
- Clinician-judgment fallback banner replaces the old generic FTS prompt
- Agent brain status mount point present (`<div id="agent-brain-status">` + `mountAgentBrainStatus()`)
- Bundled evidence corpus (1409 trials) properly referenced; stale 12840 constant removed
- Degraded-mode banner wired to indexed corpus flag

**Gated items (cannot go live without):**

1. GitHub push — SSH/HTTPS auth required to open PRs and trigger CI
2. Alembic migration must run in target environment (`uv run alembic upgrade head`)
3. `qeeg_knowledge` provider still a placeholder in the Agent Brain — evidence page can call it but gets `not_configured` response
4. `biomarker` and `assessment` providers also placeholder — not blocking for evidence page specifically
5. Indication seed data only covers 2 indications (`tdcs_asd`, `tps_chronic_pain`) — production needs full indication roster

---

## Class C Violations

None. All changes are Class A (UI/safety wiring) and Class B (new API endpoint, DB migration,
license-approved test data). No autonomous prescribing, no fake predictions, no paid APIs, no
unlicensed code.

---

## Clinical Disclaimers

All honest unavailable states are present:
- Empty PMID/DOI state: "No identifiers available"
- Empty papers state: "No linked evidence — clinician judgment required"
- Grade C/D indications carry explicit FDA note ("No FDA clearance for this indication")
- No fabricated PMID, DOI, NCT number, or dosing parameter anywhere in this work

---

## Files Changed (cumulative across all 4 phases)

Backend (t_013ee166):
- apps/api/app/services/evidence_service.py
- apps/api/app/routers/evidence.py
- apps/api/app/models/indications.py
- apps/api/alembic/versions/20260509_evidence_indication_detail.py
- apps/api/app/services/indications_seed.py
- apps/api/tests/test_evidence_indication_detail.py

Frontend (t_a588fec1):
- apps/web/src/pages-research-evidence.js
- apps/web/src/pages-research-evidence-frontend.test.js
- apps/web/src/evidence-live-wiring-regressions.test.js
- apps/web/package.json

Docs (t_6d3dda03):
- docs/evidence-deepdive-architecture.md

Final report (t_f341f371):
- docs/EVIDENCE_DEEPDIVE_REPORT_2026-05-09.md (this file)

---

*Report generated 2026-05-09 by coordinator/t_f341f371*
