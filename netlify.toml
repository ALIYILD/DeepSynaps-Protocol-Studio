[build]
  command = "npm install --no-audit --no-fund && npm run build --workspace @deepsynaps/web"
  publish = "apps/web/dist"
  base = "."

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
