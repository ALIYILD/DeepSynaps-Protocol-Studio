# DeepSynaps Protocol Studio — Environment Configuration
# Copy to .env and fill in your values.

# ── App Environment ─────────────────────────────────────────────
# Options: development | test | staging | production
DEEPSYNAPS_APP_ENV=development

# ── Database ────────────────────────────────────────────────────
# SQLite (default for dev/test): set DEEPSYNAPS_DB to file path
# PostgreSQL (required for production): set DATABASE_URL
DATABASE_URL=
DEEPSYNAPS_DB=deepsynaps.db

# ── PostgreSQL Connection Pooling ──────────────────────────────
# Only used when DATABASE_URL is set (PostgreSQL mode)
POSTGRES_POOL_SIZE=10
POSTGRES_MAX_OVERFLOW=20
POSTGRES_POOL_RECYCLE=3600
POSTGRES_POOL_PRE_PING=true
POSTGRES_SSLMODE=prefer

# ── GZip Compression ──────────────────────────────────────────
DEEPSYNAPS_ENABLE_GZIP=true
DEEPSYNAPS_GZIP_MINIMUM_SIZE=1024

# ── Demo Mode ──────────────────────────────────────────────────
DEEPSYNAPS_DEMO_MODE=false

# ── CORS Origins ───────────────────────────────────────────────
DEEPSYNAPS_CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# ── Logging ────────────────────────────────────────────────────
DEEPSYNAPS_LOG_LEVEL=INFO

# ── Demo Mode ─────────────────────────────────────────────────────────────────
# Demo/synthetic mode — controls demo data seeding and frontend banner.
# Production deployments should set DEEPSYNAPS_DEMO_MODE=false and
# DEEPSYNAPS_APP_ENV=production.
DEEPSYNAPS_DEMO_MODE=false
DEEPSYNAPS_DEMO_CLINIC_SEED=false
DEEPSYNAPS_DEMO_MODE_LABEL="DEMO BUILD"

# ── Redis Cache ────────────────────────────────────────────────
# Optional Redis caching for summary endpoints. Falls back to in-memory mock.
# REDIS_URL=redis://localhost:6379/0
# DEEPSYNAPS_ENABLE_REDIS_CACHE=false
# DEEPSYNAPS_CACHE_TTL_SECONDS=60
# DEEPSYNAPS_PATIENT_CACHE_TTL_SECONDS=60
# DEEPSYNAPS_CLINIC_SUMMARY_CACHE_TTL_SECONDS=30

# ── Security (production only) ────────────────────────────────
# DEEPSYNAPS_JWT_SECRET=your-secret-key-here
# DEEPSYNAPS_ENCRYPTION_KEY=your-encryption-key-here
