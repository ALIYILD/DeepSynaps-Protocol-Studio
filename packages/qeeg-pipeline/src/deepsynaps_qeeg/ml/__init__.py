"""ML primitives for DeepSynaps qEEG Analyzer.

Houses foundation-model embedding wrappers, brain-age prediction, and
other learned primitives. Everything here guards heavy deps and returns
deterministic stubs when imports fail — legacy pipelines keep working.
"""
